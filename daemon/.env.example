# Raptor Daemon Configuration
# The daemon runs as a standalone binary on the host machine
# Copy this file to .env and update the values

# Address the daemon listens on (use 0.0.0.0 to listen on all interfaces)
DAEMON_ADDR=0.0.0.0:8080

# API Key - MUST match the key shown when adding this daemon in the panel
# This is generated automatically when you create a daemon in the admin panel
DAEMON_API_KEY=your-daemon-api-key-from-panel

# SFTP Configuration - path where container files are stored
SFTP_BASE_PATH=/data/raptor/containers
SFTP_HOST=0.0.0.0
SFTP_PORT=2222

# Available IPs for container allocation (comma-separated for multiple)
AVAILABLE_IPS=0.0.0.0

# Logging level (error, warn, info, debug, trace)
RUST_LOG=info

# Docker socket path (default for Linux)
# DOCKER_HOST=unix:///var/run/docker.sock

DAEMON_DATA_DIR=/full/path/to/raptor/daemon/sys

TLS_CERT_PATH=/etc/letsencrypt/live/yourdomain.com/fullchain.pem
TLS_KEY_PATH=/etc/letsencrypt/live/yourdomain.com/privkey.pem